import { module, test } from 'qunit';
import { setupTest } from 'ember-qunit';

module('Unit | Adapter | assignment type', function (hooks) {
  setupTest(hooks);

  // Replace this with your real tests.
  test('it exists', function (assert) {
    assert.expect(0);
  });
});
